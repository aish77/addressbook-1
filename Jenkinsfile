pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'running the build'
      }
    }

    stage('Test') {
      steps {
        echo 'testing the app'
      }
    }

    stage('Deploy') {
      steps {
        echo 'Deploying the app'
      }
    }

  }
}